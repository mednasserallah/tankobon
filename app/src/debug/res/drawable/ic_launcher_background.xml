<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="432"
    android:viewportHeight="432">
    <!-- Debug builds get a distinct cool-tinted background so they're
         recognizable next to a release install (which uses cream). The 本
         glyph foreground is shared with the main variant. -->
    <path
        android:pathData="M0,0h432v432h-432z"
        android:fillColor="#DCE6F0" />
</vector>
